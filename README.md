## Not your father's backbone

```sh
$ npm install
```

```sh
$ webpack --watch --progress
```

open another terminal
```sh
./server.sh
```

then you launch your `chrome` and should see something in `http://localhost:9999`

Have fun.